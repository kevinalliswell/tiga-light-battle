# 安全政策

## 支持范围

当前主要支持默认分支 `main` 的最新提交。个人分支和旧版本可能不会获得修复。

## 报告问题

如果发现可能导致账户、设备、隐私或供应链风险的问题，请不要在公开 Issue 中发布可利用细节。请使用 GitHub Security Advisories（如果仓库已启用）或直接联系仓库维护者，提供：

- 受影响的提交或版本。
- 可复现步骤和最小示例。
- 影响范围和可能的修复建议。

我们会在确认问题后尽快回复，并在修复发布后公开必要的说明。

## 开发安全

- 不要提交 `.env`、令牌、私钥、个人数据或浏览器导出文件。
- 依赖变更前运行 `npm audit` 并检查锁文件差异。
- 只使用拥有明确许可的第三方依赖和素材。
